import * as Dialog from '@radix-ui/react-dialog'

export const Description = (props: Dialog.DialogDescriptionProps) => {
  return <Dialog.Description {...props} className="typography-base text-foreground-secondary" />
}
